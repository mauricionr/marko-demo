{
  "name": "marko-demo",
  "version": "1.0.0",
  "description": "Handling n promises with marko and await tag",
  "main": "server.js",
  "scripts": {
    "preinstall": "npm install -g json-server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "fake": "json-server --watch ./db.json",
    "dev": "nodemon server.js"
  },
  "author": "Mauricionr",
  "license": "MIT",
  "dependencies": {
    "express": "^4.14.1",
    "marko": "^3.14.2",
    "request": "^2.79.0"
  },
  "devDependencies": {
    "nodemon": "^1.11.0",
    "json-server": "^0.9.5"
  }
}
